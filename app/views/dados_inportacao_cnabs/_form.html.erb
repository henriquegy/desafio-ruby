<%= form_with(model: dados_inportacao_cnab) do |form| %>
  <% if dados_inportacao_cnab.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(dados_inportacao_cnab.errors.count, "error") %> prohibited this dados_inportacao_cnab from being saved:</h2>

      <ul>
        <% dados_inportacao_cnab.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="file-field input-field">
    <div class="btn">
      <span>File</span>
      <%= form.file_field :arquivo %>
    </div>
    <div class="file-path-wrapper">
      <input class="file-path validate" type="text">
    </div>
  </div>

  <div class="actions">
    <input type="submit" value="Importar documento" class="waves-effect waves-light btn cor-branca" />
    <%= link_to 'Voltar a listagem', dados_inportacao_cnabs_path, class: "waves-effect waves-light btn", style: "color:white;" %>
  </div>
<% end %>
